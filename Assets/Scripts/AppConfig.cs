using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AppConfig : MonoBehaviour
{
    //[SerializeField] public bool appRunning = false;
    [SerializeField] public bool appOperation;
    [SerializeField] public bool arUcoOperation;
    [SerializeField] public bool gazeShareOperation;
    //[SerializeField] public bool GazeTrackOperation;
    [SerializeField] public bool gazeSaveOperation;
    [SerializeField] public bool netMQOperation;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
